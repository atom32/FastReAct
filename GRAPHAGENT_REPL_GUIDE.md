# GraphAgent REPL - 使用指南

## 概述

**GraphAgent REPL** 是 FastReAct 的智能任务规划 REPL，提供了接近 Claude Code 的用户体验。

### 核心特性

1. **自动任务复杂度评估**
   - 分析查询的复杂度
   - 自动选择最适合的执行模式

2. **GraphAgent 自动规划**
   - LLM 自动生成多步骤执行计划
   - 用户确认后再执行

3. **实时进度反馈**
   - 流式输出 LLM 思考过程
   - 工具执行状态面板

4. **多模式支持**
   - AUTO：自动选择模式
   - REACT：快速响应
   - GRAPH：智能规划
   - IEL：交互式重规划（开发中）

---

## 快速开始

### 启动 REPL

```bash
# 方式 1：直接运行脚本
python scripts/run_graph_repl.py

# 方式 2：作为模块运行
python -m fastreact.cli.graph_repl
```

### 基本使用

```
FastReAct[AUTO] Q0 >> 帮我搜索最新的 AI 新闻并生成摘要

[任务复杂度: MEDIUM] (score: 0.50)
  原因: 多步骤关键词: 搜索, 生成
推荐模式: GRAPH_AGENT

[GRAPHAGENT 模式]

Step 1: 生成执行计划...

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃              执行计划                  ┃
┡━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┩
│ 目标: 搜索 AI 新闻并生成摘要             │
│ 步骤数: 2                               │
└────────────────────────────────────────┘

┏━━━━┳━━━━━━━┳━━━━━━━━━━━━━━━━━┳━━━━━━━━┓
┃ ID ┃ Tool  ┃ Description     ┃ Deps   ┃
┡━━━━╇━━━━━━━╇━━━━━━━━━━━━━━━━━╇━━━━━━━━┩
│ s1 │ Search│ 搜索 AI 新闻    │ -      │
│ s2 │ Summar│ 生成摘要        │ s1     │
└────┴───────┴─────────────────┴────────┘

是否执行该计划？ [Y/n]: Y

Step 2: 执行计划...

⠋ 执行中...

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃           执行统计                      ┃
┡━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┩
│ 总节点: 2                               │
│ 完成: 2                                 │
│ 失败: 0                                 │
│ 耗时: 2.34s                             │
└────────────────────────────────────────┘

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃              最终答案                  ┃
┡━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┩
│ [生成的摘要内容...]                     │
└────────────────────────────────────────┘
```

---

## 命令参考

### 基础命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `run <query>` | 执行查询（自动模式选择） | `run 搜索 AI 新闻` |
| `mode <name>` | 切换模式 | `mode graph_agent` |
| `stats` | 显示统计信息 | `stats` |
| `history` | 显示查询历史 | `history 10` |
| `help` | 显示帮助 | `help` |
| `exit/quit` | 退出 | `exit` |

### 快捷命令

| 命令 | 说明 |
|------|------|
| `/react` | 切换到 ReAct 模式 |
| `/graph` | 切换到 GraphAgent 模式 |
| `/iel` | 切换到 IEL 模式 |
| `/auto` | 切换到自动模式（默认） |

---

## 执行模式

### AUTO 模式（默认）

自动评估任务复杂度并选择合适的执行模式。

**评估因子**：
- 查询长度
- 多步骤关键词
- 工具数量
- 条件/循环逻辑
- 文件操作

**模式选择规则**：
```
简单任务 (score < 0.4) → REACT
中等任务 (0.4 ≤ score < 0.7) → GRAPH_AGENT
复杂任务 (score ≥ 0.7) → IEL
```

### REACT 模式

传统的 ReAct 循环，适合简单查询。

**特点**：
- 快速响应
- 自动循环
- 轻量级

**适用场景**：
- 简单问答
- 单步工具调用
- 快速验证

### GRAPH_AGENT 模式

LLM 自动规划执行计划，适合中等复杂度任务。

**流程**：
```
1. LLM 生成执行计划
2. 显示计划给用户
3. 用户确认
4. 执行计划
5. 返回结果
```

**适用场景**：
- 多步骤任务
- 需要规划的工作流
- 数据处理流水线

### IEL 模式

交互式执行循环，适合复杂任务（开发中）。

**特点**：
- 动态重规划
- 用户中断
- 快照回滚

**适用场景**：
- 代码重构
- 复杂工作流
- 需要人工干预的任务

---

## 使用示例

### 示例 1：简单查询（AUTO → REACT）

```
FastReAct[AUTO] Q1 >> 2 + 2 * 3 = ?

[任务复杂度: SIMPLE] (score: 0.00)
推荐模式: REACT

[REACT 模式]

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃              REACT Result              ┃
┡━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┩
│ 8                                      │
└────────────────────────────────────────┘
```

### 示例 2：中等复杂度（AUTO → GRAPH_AGENT）

```
FastReAct[AUTO] Q2 >> 搜索最新的 Python 3.12 新特性并生成 Markdown 报告

[任务复杂度: MEDIUM] (score: 0.60)
  原因: 多步骤关键词: 搜索, 生成, 批量操作
推荐模式: GRAPH_AGENT

[GRAPHAGENT 模式]

Step 1: 生成执行计划...

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃              执行计划                  ┃
┡━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┩
│ 目标: 搜索 Python 3.12 新特性并生成报告  │
│ 步骤数: 3                               │
└────────────────────────────────────────┘

┏━━━━┳━━━━━━━┳━━━━━━━━━━━━━━━━━┳━━━━━━━━┓
┃ ID ┃ Tool  ┃ Description     ┃ Deps   ┃
┡━━━━╇━━━━━━━╇━━━━━━━━━━━━━━━━━╇━━━━━━━━┩
│ s1 │ Search│ 搜索 Python 3.12 │ -      │
│ s2 │ Analy │ 分析新特性       │ s1     │
│ s3 │ Write │ 生成 Markdown    │ s2     │
└────┴───────┴─────────────────┴────────┘

是否执行该计划？ [Y/n]: Y

[执行结果...]
```

### 示例 3：手动模式切换

```
FastReAct[AUTO] Q3 >> /graph

[SUCCESS] 切换到 GRAPHAGENT 模式

FastReAct[GRAPH] Q3 >> 分析项目结构并生成架构图

[GRAPHAGENT 模式]
[直接进入 GraphAgent 执行...]
```

---

## 与 Claude Code 对比

| 特性 | Claude Code | GraphAgent REPL | 差距 |
|------|-------------|-----------------|------|
| **自动任务分解** | ✅ | ✅ | 无 |
| **计划可视化** | ✅ | ✅ | 无 |
| **用户确认** | ✅ | ✅ | 无 |
| **实时进度** | ✅ | ✅ | 无 |
| **文件操作原子性** | ✅ | ⚠️ 计划中 | 中 |
| **Git 集成** | ✅ | ⚠️ 计划中 | 中 |
| **失败回滚** | ✅ | ⚠️ 计划中 | 中 |
| **多模态输入** | ✅ | ❌ | 大 |
| **Web UI** | ✅ | ⚠️ 需 FastReAct-web | 中 |

### 核心差异

**Claude Code 的优势**：
1. 深度集成 Git（原子操作）
2. 更完善的多模态支持
3. 更成熟的错误处理

**GraphAgent REPL 的优势**：
1. 双模式架构（ReAct + Graph）
2. 完全本地化（隐私保护）
3. MCP 协议支持（100+ 工具）
4. 可扩展性更强

---

## 高级用法

### 自定义复杂度评估

```python
from fastreact.cli.graph_repl import ComplexityEvaluator

class CustomEvaluator(ComplexityEvaluator):
    async def evaluate(self, query: str):
        # 自定义评估逻辑
        result = await super().evaluate(query)

        # 添加自定义因子
        if "重构" in query:
            result["score"] += 0.3
            result["reasons"].append("代码重构")

        return result
```

### 集成自定义工具

```python
from fastreact.cli.graph_repl import GraphAgentREPL

repl = GraphAgentREPL()

# 添加自定义工具到 GraphAgent
agent = repl._get_or_create_graph_agent()

async def my_tool(param: str):
    return f"Result: {param}"

agent.tools["my_tool"] = my_tool
```

---

## 故障排除

### 问题 1：GraphAgent 不生成计划

**症状**：一直停留在 "生成执行计划..." 步骤

**原因**：LLM API 调用失败

**解决方案**：
1. 检查 API key 配置
2. 检查网络连接
3. 查看日志：`export FASTREACT_LOG_LEVEL=DEBUG`

### 问题 2：模式选择不准确

**症状**：简单任务被识别为复杂任务

**原因**：复杂度评估器参数需要调整

**解决方案**：
```python
# 修改复杂度阈值
if score < 0.6:  # 原来是 0.4
    complexity = "simple"
```

### 问题 3：Rich 显示异常

**症状**：终端显示乱码或布局错乱

**原因**：Windows 终端编码问题

**解决方案**：
```bash
# 设置环境变量
set PYTHONIOENCODING=utf-8

# 或使用基础模式（无 Rich）
export FASTREACT_BASIC_MODE=1
```

---

## 开发路线图

### v1.2.0（当前）

- [x] GraphAgent REPL 基础实现
- [x] 复杂度自动评估
- [x] 多模式支持
- [x] 计划可视化

### v1.3.0（规划）

- [ ] Git 集成（原子操作）
- [ ] IEL 模式完整实现
- [ ] 快照和自动回滚
- [ ] 实时进度条优化

### v2.0.0（未来）

- [ ] 多模态输入
- [ ] Web UI 集成
- [ ] 插件系统
- [ ] 分布式执行

---

## 贡献

欢迎贡献！请查看 [CLAUDE.md](CLAUDE.md) 了解开发规则。

---

**最后更新**: 2025-02-05
**版本**: v1.2.0-alpha
**状态**: Alpha - 核心功能可用，持续改进中
