# FastReAct Dependencies
# Python 3.11+ required

# Core dependencies
openai>=1.0.0
httpx>=0.25.0
pydantic>=2.0.0

# Optional dependencies
python-dotenv>=1.0.0

# Context management dependencies
tiktoken>=0.5.0  # For accurate token counting (optional, falls back to estimation)

# Memory/Vector search dependencies
sqlite-vec>=0.1.0  # Vector similarity search in SQLite (optional, for long-term memory)
apsw>=3.51.0  # Windows-compatible SQLite wrapper (for sqlite-vec on Windows)
sentence-transformers>=5.0.0  # Local embeddings (alternative to API-based)

# Gateway dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0

# Storage dependencies
aiosqlite>=0.19.0

# Development dependencies (optional)
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# pytest-cov>=4.0.0
# black>=23.0.0
# ruff>=0.1.0
