version: '3.8'

services:
  apollo-mcp:
    build:
      context: .
      dockerfile: test_docs/Dockerfile
    container_name: apollo-mcp-server
    restart: unless-stopped
    stdin_open: true
    tty: true
    networks:
      - fastreact-network

networks:
  fastreact-network:
    driver: bridge
